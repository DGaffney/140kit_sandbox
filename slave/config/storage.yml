test:
  type: local
  path: results_test #This will always be relative to project root.

development:
  type: local
  path: results #This will always be relative to project root.

production:
  type: remote
  path: results
  user: some_user_on_remote_machine
  host: some.domain.name  

  