<h1><%= @researcher.user_name %></h1>
  <p>
    <strong>Role:</strong> <%=h @researcher.role %><br />
   <strong>Hailing from:</strong> <%=h @researcher.location %><br />
   <% if @researcher.share_email %>
    <strong>E-mail:</strong> <%=h @researcher.email %><br />
   <% end %>
  <% if !@researcher.website_url.nil? %>
    <%= link_to @researcher.website_url.to_url, @researcher.website_url %><br />
  <% end %>
    <strong>Joined:</strong> about <%= time_ago_in_words(@researcher.join_date) %> ago
  </p>
<h2><%= link_to "Add a new dataset",  url(:dataset, :new)%></h2>
<h2><%= link_to "Add a new curation",  url(:dataset, :new)%></h2>
<div>
<h2>Curations</h2>
<%= partial 'curations/index', :researcher => @researcher.user_name, :finished => false %>

<h2>Datasets</h2>
<%= partial 'datasets/index', :researcher => @researcher.user_name, :finished => false %>
